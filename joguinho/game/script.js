    document.addEventListener("keydown", function(event) {
        if (event.key === "Escape") { // Verifica se a tecla pressionada é o ESC
            window.location.href = "../index.html"; // Redireciona para o menu do jogo
        }
    });
